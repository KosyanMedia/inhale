{
    "file*log": {
        "series": {
            "regexp": "(?P<value>.*?) (?P<name>.*)",
            "const": {
                "host": "ubuntu"
            },
            "numeric_fields": ["value"],
            "eval": {"val2": "value * 2"}
        }
    }
}
