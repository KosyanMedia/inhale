{
    "l*log": {
        "log_lines_1": {
            "regexp": "(?P<value>.*?) (?P<name>.*)",
            "const": {
                "host": "ubuntu"
            },
            "numeric_fields": ["value"],
            "eval": {"val2": "value * 2"}
        }
    }
}
